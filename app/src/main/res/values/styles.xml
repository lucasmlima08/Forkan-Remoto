<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Estilo do botão de categorias. -->
    <style name="estiloBotaoCategorias" parent="android:Widget.Button">
        <item name="android:textSize">@dimen/categoria</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:height">@dimen/botaoCategorias_tamanhoVertical</item>
        <item name="android:layout_marginBottom">@dimen/botaoCategoras_margemAbaixo</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:clickable">true</item>
    </style>

</resources>
